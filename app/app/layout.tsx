export const metadata = {
  title: "Humanverse AI"
}

export default function Rootlayout({
  children,
} : {
  Children: React.ReactNode
}) {
  return (
    <html>
    <body>(children)</body>
    </html>
    )
}
